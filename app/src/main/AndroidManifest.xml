<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.khalid.gads20leaderboard">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".SubmissionAborted"
            android:configChanges="orientation"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.SubmissionDialog"
           />

        <activity android:name=".SuccessfulSubmission"
            android:configChanges="orientation"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.SubmissionDialog"/>

        <activity
            android:name=".SubmissionDialog"
            android:configChanges="orientation"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.SubmissionDialog" />

        <activity android:name=".SubmissionActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>

        <activity android:name=".HomeActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:screenOrientation="portrait"/>

        <activity android:name=".SplashScreenActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>